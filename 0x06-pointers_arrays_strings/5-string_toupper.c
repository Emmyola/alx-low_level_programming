#include "main.h"
#include <string.h>

/**
 * String_toupper -> converting an string to array
 * @X: string param
 * Return: string
 */

char *string_toupper(char *)
{
int i = 0;
while (x[i])
{
if (x[i] >= 97 && x[i] <= 122)
x[i] = x[i] - 32;
i++;
}
return (x);
}

